prefix_rule(
    pattern = ["rm"],
    decision = "forbidden",
    justification = "The global profile forbids rm. Use a reversible or narrowly scoped alternative.",
    match = [
        "rm file.txt",
        "rm -rf build",
    ],
    not_match = [
        "rmdir empty-directory",
        "unlink file.txt",
    ],
)
